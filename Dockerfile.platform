FROM golang:1.22 AS build
WORKDIR /src
COPY go.mod ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/envoy-platform ./cmd/platform

FROM gcr.io/distroless/static-debian12
COPY --from=build /out/envoy-platform /envoy-platform
EXPOSE 8080
ENTRYPOINT ["/envoy-platform"]
