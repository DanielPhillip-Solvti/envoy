See makefile for local commands
usage example: "make run-platform"

the nats server should be started first, then platform, then agent.

The platform and nats server can be run easily using 'docker compose up'

Using the docker compose method pulls the latest platform image so may not be as good for local development
